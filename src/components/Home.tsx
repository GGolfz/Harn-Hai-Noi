import { useState } from "preact/hooks";

const Home = () => {
    const [groups, setGroups] = useState([]);

    return (
        <>

            <button>Add new group</button>
        </>
    )
}

export default Home;